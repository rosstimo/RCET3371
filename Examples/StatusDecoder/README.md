# Status Decoder Example

Demonstrates extracting and replacing packed fields while preserving unrelated bits.

Run C#:

```bash
dotnet run --project StatusDecoder.csproj
```

Run Python:

```bash
python status_decoder.py
```

Expected first line from both:

```text
mode=3 fault=0 sequence=12
```
