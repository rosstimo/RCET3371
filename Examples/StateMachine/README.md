# Controlled-Time State Machine Example

Demonstrates a small controller driven by supplied elapsed time rather than `Sleep`.

Run:

```bash
dotnet run --project StateMachine.csproj
```

Expected output:

```text
Heating
Heating
Idle
```
